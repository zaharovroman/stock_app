import 'package:flutter/material.dart';
import 'app_colors.dart';

class Fonts {
  static const stockSymbol = TextStyle(color: AppColors.stockSymbolText);
  static const stockTitle = TextStyle(fontWeight: FontWeight.w600);
  static const stockCurrency = TextStyle(
    fontSize: 16,
    fontWeight: FontWeight.w700,
  );
}
